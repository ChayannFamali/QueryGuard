apiVersion: v1
kind: Secret
metadata:
  name: queryguard-secret
  namespace: queryguard
type: Opaque
# IMPORTANT: Base64-encode real values before applying, or use External Secrets / SealedSecrets.
# The values below are placeholders — do NOT use in production.
stringData:
  dashboard-username: "REPLACE_ME"
  dashboard-password: "REPLACE_ME"
  metrics-username: "REPLACE_ME"
  metrics-password: "REPLACE_ME"
